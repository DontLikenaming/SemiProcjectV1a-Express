{{#section 'css'}}
    <style>
        .myinfo {
            border-collapse: collapse;
            width: 300px;
            margin: 0 auto
        }
        .myinfo td {
            padding: 5px;
        }
        .myinfo td:first-child{
            font-weight: bold;
            text-align: right
        }
    </style>
{{/section}}
{{#section 'script'}}
{{/section}}
<h3>회원정보</h3>
<table class="myinfo">
    <tr><td>아이디</td>{{#mem}}<td>{{userid}}</td>{{/mem}}</tr>
    <tr><td>이름</td>{{#mem}}<td>{{name}}</td>{{/mem}}</tr>
    <tr><td>이메일</td>{{#mem}}<td>{{email}}</td>{{/mem}}</tr>
    <tr><td>가입일</td>{{#mem}}<td>{{regdata}}</td>{{/mem}}</tr>
</table>
